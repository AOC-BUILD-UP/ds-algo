{
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "gts": "^5.2.0",
    "jest": "^29.7.0",
    "typescript": "~5.2.0",
    "@types/node": "20.8.2"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "test": "jest",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
